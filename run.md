# Running LeaseLens Locally

## Frontend Setup
1. `cd frontend`
2. `npm install`
3. `npm run dev`

## Backend Setup (Docker)
1. `docker-compose up --build`

## Showcase Mode
The frontend is currently configured to run in **Showcase Mode** by default, intercepting all API calls with mocked responses. No backend is required for the UI demo.
